{
  "name": "vue_printer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir extension",
    "build:dev": "vite build --mode development --outDir extension",
    "build:debug": "vite build --mode development --debug --outDir extension",
    "preview": "vite preview",
    "build:extension": "vite build --mode extension --outDir extension",
    "dev:extension": "vite --mode extension"
  },
  "dependencies": {
    "vue": "^3.5.32",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.1.4",
    "vite-plugin-vue-devtools": "^8.1.1"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}